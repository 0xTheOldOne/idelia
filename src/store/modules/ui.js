/**
 * Module Vuex — ui.
 *
 * État d'interface **volatile**, non persisté (contrairement aux autres
 * modules). Ossature vide de la feature 001.
 */
export default {
  namespaced: true,
  state: () => ({}),
  getters: {},
  mutations: {},
  actions: {},
};
